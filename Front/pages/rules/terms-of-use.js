import React from 'react';
import useTitle from '../../hooks/useTitle';

const Terms = () => {
    useTitle("Terms of Use")

    return (
        <div>
            Terms of use
        </div>
    );
};

export default Terms;